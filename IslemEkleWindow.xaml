<Window x:Class="BenimFinansim.IslemEkleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yeni İşlem" Height="480" Width="400" WindowStartupLocation="CenterOwner"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize"
        FontFamily="Poppins, Segoe UI, sans-serif">
    
    <Window.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="White" BorderThickness="0" CornerRadius="8">
                                            <Path x:Name="Arrow" Fill="#64748B" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" Data="M 0 0 L 5 5 L 10 0 Z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" Margin="15,0,35,0" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#CBD5E1" CornerRadius="8" Margin="0,4,0,0" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Margin="0,5" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="White" CornerRadius="15" Margin="15" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.1" Direction="270" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/> 
                <RowDefinition Height="*"/>  
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="15,15,0,0" MouseLeftButtonDown="Window_MouseDown">
                <Grid>
                    <TextBlock Text="Yeni İşlem Ekle" FontSize="16" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <Button Content="✕" Width="32" Height="32" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0"
                            Background="White" BorderThickness="1" BorderBrush="#E2E8F0" Foreground="#64748B" FontSize="14" FontWeight="Bold"
                            Click="BtnClose_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
            <Rectangle Grid.Row="0" Height="1" Fill="#E2E8F0" VerticalAlignment="Bottom"/>

            <StackPanel Grid.Row="1" Margin="30,25,30,30">
                
                <TextBlock Text="Başlık (Örn: Market, Fatura)" FontSize="13" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,8"/>
                <Border Background="White" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" Height="45" Margin="0,0,0,20">
                    <TextBox x:Name="txtBaslik" Background="Transparent" BorderThickness="0" FontSize="15" VerticalContentAlignment="Center" Margin="15,0" Foreground="#1E293B" FontWeight="SemiBold"/>
                </Border>

                <TextBlock Text="Miktar (TL)" FontSize="13" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,8"/>
                <Border Background="White" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" Height="45" Margin="0,0,0,20">
                    <TextBox x:Name="txtMiktar" Background="Transparent" BorderThickness="0" FontSize="16" VerticalContentAlignment="Center" Margin="15,0" Foreground="#1E293B" FontWeight="SemiBold"/>
                </Border>

                <TextBlock Text="Kategori" FontSize="13" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,8"/>
                <Border Background="White" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" Height="45" Margin="0,0,0,30">
                    <ComboBox x:Name="cmbKategori" SelectedIndex="0" VerticalContentAlignment="Center">
                        </ComboBox>
                </Border>

                <Button Content="İşlemi Kaydet" Click="Kaydet_Click" Background="#3B82F6" Foreground="White" 
                        FontWeight="Bold" FontSize="16" Height="50" Cursor="Hand" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
            </StackPanel>
        </Grid>
    </Border>
</Window>